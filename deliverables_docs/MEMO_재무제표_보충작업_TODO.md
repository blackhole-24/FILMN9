# 📌 재무상태표·손익계산서 보충 작업 메모 (TODO)

> **작성일**: 2026-05-27
> **재개 시점**: 사용자가 "재무상태표 손익계산서 다시 확인" 라고 말할 때
> **목적**: 1차 작업의 한계 + 나중에 보충할 방법 기록

---

## 🎯 현재 상태 요약 (2026-05-27 15:15 시점)

### ✅ 완료된 작업
| 항목 | 결과 |
|------|------|
| 데모 3종목 완전 데이터 | 아모레·삼성전기·NAVER → 3개년 BS·CIS 연결+별도 ✅ |
| DB 적재 종목 | **1,861개** (625,218건) |
| XML 다운로드 추가 | data/raw 654 → 2,204개 |
| 백엔드 수정 | 음수 처리 + 매출액·자산총계 우선 정렬 |
| OpenAI 비용 | **$0** (사용 안 함) |

### ⚠️ 한계 (보충 필요)
| 항목 | 종목 수 |
|------|--------|
| **외감법인** (사업보고서 자체 없음) | 약 529개 |
| **JSONL 보유 + XML 없음** (RAG만 있고 정형 데이터 없음) | 약 27개 |
| **DB 적재 안 됨** | 약 700개 |

---

## 📊 형식별 보유 종목 — 최종 매트릭스

| 형식 | 종목 수 | 위치 |
|------|:------:|------|
| **JSONL (4팀 RAG)** | 2,596개 | `data/RAG/` + 4팀 폴더 |
| **XML 사업보고서** | 약 1,973개 | `data/raw/` + `data/팀원1_DART/DART/raw/` |
| **XML 감사보고서 (잘못)** | 529개 | `data/raw/` (사업보고서 데이터 없음) |
| **임베딩 (Chroma)** | 2,596개 (JSONL과 동일) | `data/embeddings/chroma_db/` (20GB) |
| **DB 적재** | 1,861개 | `data/filmn9.db` financial_detail 테이블 |

---

## 🛠️ 나중에 보충할 방법 (다음 작업 시 진행)

### 옵션 1: JSONL 청크에서 표 텍스트 파싱 ⭐
- **대상**: 외감법인 529개 + JSONL만 보유 27개 = **556개**
- **방법**:
  - `data/RAG/{종목}_2025_annual_chunks.jsonl` 안 `kind="table"` 청크
  - 마크다운 표 형식 → 정규식·LLM 파싱
  - 재무상태표·손익계산서 행 추출
- **정확도**: ⭐⭐⭐ (텍스트 변환 손실 일부)
- **OpenAI 필요 여부**: gpt-5-mini 사용 시 비용 발생 ⚠️ (사전 협의 필요)

### 옵션 2: 임베딩(Chroma) 검색 후 추출
- **대상**: 2,596개 임베딩 보유 종목
- **방법**:
  - `data/embeddings/chroma_db/` Chroma 벡터 검색
  - 쿼리: "재무상태표 자산총계" "포괄손익계산서 매출액"
  - 유사 청크 가져와서 텍스트 파싱
- **정확도**: ⭐⭐⭐
- **OpenAI 필요**: 임베딩 쿼리만 (저렴) — 약 $1~5 예상

### 옵션 3: DART API 다양한 reprt_code 시도
- **대상**: 외감법인 529개
- **방법**:
  - `reprt_code` 변경 (11011 외 다른 옵션)
  - 분기보고서·반기보고서 시도
  - 또는 외감법인용 별도 API
- **정확도**: ⭐⭐⭐⭐ (받기만 하면 정확)
- **성공 가능성**: 낮음 (외감법인은 본질적으로 사업보고서 없음)

### 옵션 4: KIND 공시 시스템에서 재무 별도 다운로드
- **대상**: 외감법인 + 누락 종목
- **방법**:
  - 한국거래소 KIND (kind.krx.co.kr) 공시 검색
  - 외감법인 재무제표 PDF/HWP 다운로드
  - OCR + 파싱
- **정확도**: ⭐⭐ (OCR 손실)
- **시간**: 매우 큼

---

## 🔑 핵심 파일 위치 (재개 시 참고)

| 파일 | 역할 | 라인 |
|------|------|:----:|
| `parse_business_report_xml.py` | XML 파싱 + DB 적재 | - |
| `unified_financial_loader.py` | XML 우선 + DART API 폴백 통합 | - |
| `fix_audit_via_dart_api.py` | 외감법인 DART API 시도 (실패 확인됨) | - |
| `download_missing_xml.py` | DART API XML 다운로드 (감사보고서 혼재) | - |
| `redownload_audit.py` | 감사보고서 → 사업보고서 재시도 (rate limit 실패) | - |
| `api/routers/extras.py` | 백엔드 API (음수 처리·우선정렬) | 121~210 |
| `data/filmn9.db` | SQLite DB (financial_detail 테이블) | - |
| `data/corp_code_map.json` | 종목코드↔corp_code 매핑 | - |

---

## 📋 보충 작업 진행 시 권장 순서

### Phase 1. 현황 재확인 (5분)
```bash
python check_data_freshness.py
```

### Phase 2. JSONL 청크 파싱 시도 (30분)
- 신규 스크립트 작성: `parse_jsonl_chunks_to_db.py`
- 외감법인 529개 종목의 jsonl 처리
- 표 텍스트에서 재무 데이터 추출

### Phase 3. 임베딩 검색 시도 (옵션, 30분)
- Chroma 벡터 DB 활용
- 누락 종목별 재무제표 청크 검색

### Phase 4. 검증 (10분)
- 데모 3종목 정확성 재확인
- 신규 적재 종목 샘플 검증

---

## 🎯 사용자 의도 (기록)

> "재무상태표 손익계산서 나중에 더 보충 하도록 하고,
>  나중에 다시 데이터 수집 수 다시 물어보고 xml과 json, 임베딩 파일 섞어서라도
>  모든 기업에 대해 정확하게 전부 화면에 나올 수 있도록 확인까지 하는 작업 수행"

→ **3가지 데이터 형식 결합 (XML + JSON + 임베딩)** 으로 전체 종목 정확성 확보

→ **OpenAI API 사용 시 사전 협의** 필수 (잔액 $50)

---

## 📌 재무제표 데이터 신뢰도 등급

| 데이터 출처 | 신뢰도 | 종목 수 | 비고 |
|------------|:------:|:------:|------|
| **사업보고서 XML 파싱 (1차 적재)** | ⭐⭐⭐⭐⭐ | 약 1,800개 | 사업보고서 100% 일치 |
| **DART API JSON (NAVER 등)** | ⭐⭐⭐⭐ | 약 50개 | DART가 정제한 정형 데이터 |
| **JSONL 청크 텍스트 파싱** (TODO) | ⭐⭐⭐ | 약 556개 | 텍스트 변환 손실 |
| **임베딩 검색 후 추출** (TODO) | ⭐⭐⭐ | 보충용 | LLM 의존 |
| **수동 입력·OCR** | ⭐⭐ | 최후 수단 | 시간 큼 |

---

## 📅 다음 작업 진행 시 알림 받을 트리거

사용자가 다음 표현 사용 시 이 메모 참조:
- "재무상태표 다시 확인"
- "손익계산서 다시 확인"
- "재무제표 보충"
- "XML JSON 임베딩 섞어서"
- "외감법인 처리"

---

© FILMN9 · 2026-05-27 · 다음 작업 재개 시점 대기
