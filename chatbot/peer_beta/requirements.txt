# peer_beta 모듈 의존성
# dart-rag conda 환경에서 설치:
#   conda activate dart-rag
#   pip install -r peer_beta/requirements.txt

requests>=2.31
python-dotenv>=1.0
pandas>=2.0
numpy>=1.24
statsmodels>=0.14     # OLS 회귀
matplotlib>=3.7       # 노트북 시각화 (검증용)

# Jupyter (이미 깔려있으면 생략)
jupyter
ipykernel
