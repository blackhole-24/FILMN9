# 관리자 · QA 콘솔 인수인계서 (밸류에이션 · 챗봇)

> 작성: 밸류에이션·챗봇 파트 (유태상)
> 대상: 통합/프론트 담당 팀원
> 목적: 평가위원 검토용 **관리자 페이지 + QA 테스트 페이지**를 실제 서비스에 통합·구동

---

## 0. 한 줄 요약
밸류에이션·챗봇을 **QA 관점에서 검증**하고, **평가에 쓰인 데이터 기준일·출처·테스트 환경·AI 하네스(통제 원칙)** 를 평가위원에게 보여주는 **2탭 관리자 콘솔**입니다.
- 라우트: 프론트 **`/admin`** · 백엔드 메타 **`GET /api/admin/meta`**

## 1. 구성 파일 (이 커밋에 포함)
| 파일 | 역할 |
|---|---|
| `frontend/app/admin/page.tsx` | 관리자/QA 페이지 — 단일 클라이언트 컴포넌트, 다크 테마, 인라인 스타일(외부 의존 최소) |
| `backend/routers/admin.py` | `GET /api/admin/meta` — `valuation.db`·`filmn9.db`에서 기준일·규모·출처일자 **실데이터** 집계 |
| `backend/main.py` | `admin_router` 등록(2줄) |
| `관리자페이지_인수인계서.md` | 본 문서 |

## 2. 화면 구성
**📋 관리자 탭** (평가위원 검토용)
1. **데이터 기준일** — 밸류(평가일·모델버전·Rf·KOFIA·XBRL연도·종목수) / 기업개요(주가일·재무연도) / 챗봇(청크수·임베딩모델·GPU). `/api/admin/meta` + 챗봇 `/health`로 **실시간 표시**
2. **데이터 출처** — DART·KRX·ECOS·KOFIA·한공회·Damodaran·OpenAI·BGE-M3·Chroma
3. **테스트 환경** — 밸류 엔진 방법론 / 챗봇 RAG 파이프라인 / 시스템 포트·DB·GPU
4. **AI 하네스(통제 원칙)** — 아래 5절

**🧪 테스트 탭** (QA 검증용)
- 시스템 헬스(백엔드·챗봇·프론트 신호등) · **밸류 QA**(종목코드→실측·판정 5종) · **챗봇 QA**(질의→답변·출처) · QA 체크리스트

## 3. 통합 방법 (팀원이 할 일)
### 백엔드 (FastAPI)
1. `backend/routers/admin.py` 추가 (이 커밋에 포함)
2. `backend/main.py`에 등록 (이 커밋에 포함):
   ```python
   from backend.routers.admin import router as admin_router
   app.include_router(admin_router, prefix="/api")
   ```
3. CORS는 기존 `main.py`의 `allow_origins=["*"]`로 충분.

### 프론트 (Next.js App Router)
1. `frontend/app/admin/page.tsx` 추가 → 자동으로 `/admin` 라우트 생성. **추가 라우팅 설정 불필요.**
2. **빌드 의존성**: 레포의 `frontend/components/ui/*`가 `@/lib/utils`의 `cn`을 import합니다. `frontend/lib/utils.ts`가 없으면 프로덕션 빌드(`next build`)가 실패하니 아래 표준 파일을 생성하세요(개발 `next dev`로 `/admin`만 볼 땐 불필요):
   ```ts
   // frontend/lib/utils.ts
   import { clsx, type ClassValue } from "clsx"
   import { twMerge } from "tailwind-merge"
   export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)) }
   ```
   (관리자 페이지 자체는 인라인 스타일이라 `cn`을 쓰지 않지만, 전체 빌드를 위해 필요합니다.)

### 환경변수 (`frontend/.env.local`)
```
NEXT_PUBLIC_API_URL=http://localhost:8090        # 백엔드
NEXT_PUBLIC_CHAT_API_URL=http://localhost:8800   # 챗봇
```

## 4. 데이터·엔드포인트 의존성
| 호출 | 용도 | 비고 |
|---|---|---|
| `GET /api/admin/meta` | 데이터 기준일·규모 | `valuation.db`·`filmn9.db` 없으면 해당 필드 `null`(graceful) |
| `GET /api/valuation/{code}` | 테스트 탭 밸류 QA | ★ **신규 통합 JSON 포맷 가정**(`is_mock`·`summary`·`valuation_diagnostics`). 구 6파일 포맷이면 QA 판정이 ✗로 표시됨 → 밸류 엔진 통합본과 함께 적용 권장 |
| `GET /health` | 백엔드 헬스 | |
| `GET {CHAT_API}/health` | 챗봇 청크수·GPU | 챗봇(8800) 미기동 시 신호등 빨강 |
| `POST {CHAT_API}/chat` | 챗봇 QA 질의 | reasoning='high'라 응답이 느릴 수 있음(페이지에 경고 표기) |

> `admin.py`의 DB 경로는 기본 `<repo>/data/{valuation.db,filmn9.db}`이며 환경변수 `VALUATION_DB_PATH`로 재정의 가능.

## 5. AI 하네스 (AI 통제 원칙) — 팀장 요청 사항, 페이지에 수록
**우리가 현재 AI를 어떻게 통제(조절)하는가 (개발자 관점)**
- **NO-MOCK**: 데이터 없으면 임의값 금지 → 가짜 숫자 생성 차단
- **추출·판단 분리**: LLM은 사실 추출만, 최신 선택 등 결정은 결정론적 코드
- **RAG 그라운딩 + 출처 강제**: 답변은 검색 청크 근거 + 출처카드
- **저온도(0.1)·구조화 출력**, **검색 정밀화(하이브리드·리랭킹·FINSTMT)**, **신뢰도 등급화(A~E)**, **eval 골든셋**

**더 나은 하네스 조절 제안(개선안)**
- 근거기반 거부(grounded refusal) · 클레임 단위 인용+사후 검증 · 구조화 출력 전면화 · 적대적 자기검증 · 프롬프트 버전관리+골든셋 CI · reasoning_effort 동적화 · 비용/지연 텔레메트리 · 범위 가드레일 · 프롬프트 인젝션/PII 방어

## 6. 실행·검증
```bash
# 백엔드
python -m uvicorn backend.main:app --port 8090
# 프론트
cd frontend && npm install && npm run dev
# 확인
http://localhost:3000/admin            # 관리자/테스트 2탭
http://localhost:8090/api/admin/meta   # 메타 JSON
```
검증 포인트: 관리자 탭에 기준일·종목수 표시 / 테스트 탭 헬스 신호등 / 밸류 QA(예: 009150) 판정 5종 / 챗봇 QA 응답+출처.

## 7. 커스터마이즈 포인트
- 출처/하네스 문구: `page.tsx`의 `SOURCES`, `HARNESS_DEV/USER/IMPROVE` 배열 수정
- 색상: `T` 객체 / 데모 종목: 테스트 탭 안내 문구
- 메타 항목 추가: `admin.py`에 쿼리 추가 → `page.tsx` 관리자 탭에 KV 추가

## 8. 검증 완료 상태 (작성 시점)
- `npx tsc --noEmit` 통과 · `next build` 성공(`○ /admin` 라우트 생성)
- 브라우저 렌더: 관리자 탭 4섹션 + 실데이터, 테스트 탭 헬스·밸류 QA(5/5 통과) 확인
