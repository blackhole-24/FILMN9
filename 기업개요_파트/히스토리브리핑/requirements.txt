# FILMN9 자동화 파이프라인 의존성

# 핵심
python-dotenv>=1.0.0       # .env 로드
requests>=2.31.0           # HTTP 호출 (DART API)
pandas>=2.0.0              # 데이터 처리
tqdm>=4.65.0               # 진행률 표시

# 종목 데이터
finance-datareader>=0.9.50 # KRX 종목 자동 수집 (import: FinanceDataReader)
beautifulsoup4>=4.12.0     # 네이버 WICS 크롤링
lxml>=5.0.0                # XML 파싱 + bs4 가속

# LLM
openai>=1.0.0              # GPT 호출

# MongoDB
pymongo>=4.6.0
certifi>=2024.0.0          # SSL 인증서

# 환경 (선택)
# boto3>=1.34.0            # AWS S3 (To-be Phase 2)
